# Données sensibles - NE JAMAIS committer
data/creditcard.csv

# Modeles ML : on les commite volontairement (quelques Mo, pas comme le CSV
# ci-dessus). Necessaire pour que la CI (.github/workflows/ci.yml) puisse
# charger un vrai modele et faire passer api/tests/test_api.py sans avoir a
# re-entrainer (impossible en CI : le dataset n'est pas dans le repo).
# models/*.pkl
# models/*.joblib

# Environnement Python
venv/
__pycache__/
*.pyc
.env
*.db

# Jupyter
.ipynb_checkpoints/

# Node
dashboard/node_modules/
dashboard/dist/

# Docker
*.log

*.sqlite